<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="desenvolve.unesc.myapplication">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.BancoExemplo">
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"/>
        <activity android:name=".RegisterActivity"/>
        <activity android:name=".GasolinaActivity"/>
        <activity android:name=".EntreterimentoActivity"/>
        <activity android:name=".RefeicaoActivity"/>
        <activity android:name=".TarifaAereaActivity"/>
        <activity android:name=".HospedagemActivity"/>
        <activity android:name=".ViagensActivity"/>
        <activity android:name=".ViagemActivity"/>
    </application>

</manifest>